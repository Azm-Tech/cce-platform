// PLACEHOLDER — replaced by Task 0.4 (real impl wraps MatSnackBar + ngx-translate).
import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ToastService {
  error(_messageKey: string): void {
    // No-op until Task 0.4.
  }

  success(_messageKey: string): void {
    // No-op until Task 0.4.
  }
}
