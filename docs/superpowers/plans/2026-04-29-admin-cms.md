# CCE Sub-Project 05 — Admin CMS — Implementation Plan (Master)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan phase-by-phase. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the Angular admin application (`apps/admin-cms`) — ~30 admin screens covering BRD §4.1.19–4.1.29, with BFF cookie auth, permission-gated routes, ngx-translate ar/en + RTL/LTR, typed Reactive Forms, and axe-clean accessibility. Consumes the Internal API (sub-project 3) via the generated `libs/api-client`.

**Architecture:** Angular 19 standalone components, signals-first state, lazy-loaded feature folders inside `apps/admin-cms/src/app/features/`. Cross-cutting (`core/`) holds interceptors, services, guards, and the layout shell. Per-feature `*ApiService` wrappers around the generated client; hybrid HTTP error handling (global interceptor for cross-cutting + per-feature wrappers for domain errors).

**Tech Stack:** Angular 19, Angular Material 18, Bootstrap grid, ngx-translate, Jest (unit), Cypress (E2E), axe-core (a11y). All already in `frontend/package.json` from Foundation.

**Spec reference:** [`../specs/2026-04-29-admin-cms-design.md`](../specs/2026-04-29-admin-cms-design.md) — 10 sections, 9-phase plan, ~58 tasks.

---

## Plan organization

This plan is split into 9 phase files under [`2026-04-29-admin-cms/`](./2026-04-29-admin-cms/).

| # | Phase | File | Tasks | Purpose |
|---|---|---|---|---|
| 0 | Cross-cutting | `phase-00-cross-cutting.md` | 7 | Interceptors (auth, server-error, correlation-id), AuthService + PermissionGuard + `[ccePermission]` directive, i18n + RTL toggle, ToastService + ConfirmDialogService + ErrorFormatter, `<cce-shell>` layout, `<cce-paged-table>` shared component, axe-core test harness |
| 1 | Identity admin | `phase-01-identity.md` | 6 | Users list/detail, role-assign dialog, state-rep-assignment list/create/revoke |
| 2 | Expert workflow | `phase-02-experts.md` | 4 | Expert requests list/approve/reject, expert profiles list |
| 3 | Content — resources + assets | `phase-03-content-resources.md` | 7 | Asset upload widget, resources CRUD + publish, country-resource queue + approve/reject |
| 4 | Content — news + events + pages + homepage | `phase-04-content-publishing.md` | 9 | 4 entities × CRUD + workflow buttons + homepage reorder UI |
| 5 | Taxonomies + community moderation | `phase-05-taxonomies-moderation.md` | 6 | Resource categories + topics CRUD; community posts/replies moderation queues |
| 6 | Country admin + notifications admin | `phase-06-country-notifications.md` | 6 | Countries list/edit, country profiles editor, notification templates CRUD |
| 7 | Reports | `phase-07-reports.md` | 8 | Reports landing page with 8 streaming-CSV download cards + filter dialog |
| 8 | Audit log + ADRs + release | `phase-08-audit-release.md` | 6 | Audit log query page, 4 ADRs (0035–0038), `docs/admin-cms-completion.md`, CHANGELOG, `admin-cms-v0.1.0` tag |

**Total:** ~58 tasks across 9 phases.

---

## Global conventions

### Working directory

All paths relative to repo root `/Users/m/CCE/`. Most frontend commands run from `frontend/`. The plan calls out `cd frontend` where needed.

### Git workflow

- One commit per task (atomic, reviewable history).
- Conventional commit format: `<type>(<scope>): <subject>`. Scopes for sub-project 5: `feat(admin-cms)`, `feat(ui-kit)`, `feat(auth)`, `feat(api-client)`, `test(admin-cms-e2e)`, `chore(sub-5)`, `docs(sub-5)`.
- Commit with `git -c commit.gpgsign=false commit -m "..."`. No `--no-verify`.
- Gitleaks pre-commit hook stays active.

### TDD discipline (per ADR-0007)

**Strict TDD** for:
- HTTP interceptors (mocked HttpClient via HttpTestingController).
- Pipes + utility services (small surface).
- `[ccePermission]` directive logic.
- `ErrorFormatter` mapping rules.

**Test-after** for:
- Page components (TestBed-rendered with stubbed services; cover the happy path + the obvious error branches).
- E2E flows (Cypress tests assert end-to-end behavior; not TDD-driven).

### Coverage gates

- Frontend ≥ 60% line.
- Services + pipes ≥ 80%.
- Critical/serious axe-core findings: 0.

### Versions

No new packages. Foundation locked Angular 19 + Material 18. CPM is for backend NuGet; frontend uses `pnpm` with `package.json` versions already in place.

### File-structure guideline

Each feature folder follows this convention:

```
apps/admin-cms/src/app/features/<area>/
├── routes.ts                      # IDENTITY_ROUTES, EXPERTS_ROUTES, etc.
├── <area>-api.service.ts          # wraps generated *Api
├── <name>-list.page.ts/.html/.scss/.spec.ts   # list page
├── <name>-detail.page.ts/.html/.scss/.spec.ts # detail/edit page
├── <name>-create.dialog.ts/...   # modal forms (when applicable)
└── <area>.types.ts                # local DTO/error types
```

Lazy load via `loadChildren: () => import('./features/<area>/routes').then(m => m.<AREA>_ROUTES)`.

### Verify steps

Every task has a verify step:
- `cd frontend && pnpm nx test admin-cms` — runs Jest unit tests for the app.
- `cd frontend && pnpm nx build admin-cms` — production build.
- `cd frontend && pnpm nx lint admin-cms` — eslint.
- `cd frontend && pnpm nx e2e admin-cms-e2e` — Cypress E2E suite (smoke + axe-core).

If verify fails, **stop** — re-read the plan, fix carefully or escalate.

---

## Self-review against spec

| Spec section | Phase(s) |
|---|---|
| §3.1 Layer placement | All phases — features under `app/features/`, cross-cutting in `app/core/` |
| §3.2 Signals + RxJS state | Phase 0 (AuthService); reused per feature |
| §3.3 Lazy-loaded feature folders | Phase 0 (root routes wiring); per phase (feature routes) |
| §3.4 Typed Reactive Forms | Phase 0 (ErrorFormatter); per feature |
| §3.5 Hybrid HTTP error handling | Phase 0 (interceptors + ErrorFormatter); per feature wrappers |
| §3.6 Generated client wrapping | Phase 0 (toFeatureError pattern); per feature `*ApiService` |
| §3.7 i18n + RTL/LTR | Phase 0 |
| §3.8 Permission gating | Phase 0 (Guard + directive); per route |
| §3.9 Layout shell | Phase 0 |
| §3.10 Generic paged-table | Phase 0 |
| §3.11 Testing harness | Phase 0 (axe wiring); ongoing |
| §3.12 Dev server proxy | Phase 0 |
| §4 Critical user flows | Verified by E2E across phases |
| §5 Endpoint coverage map | Phases 1–8 |
| §6 Error handling | Phase 0 |
| §7 ADRs (0035–0038) | Phase 8 |
| §9 DoD | Phase 8 verification + tag |

Every spec section maps to at least one phase. Self-review: **complete**.

---

## Execution handoff

Two execution options:

**1. Subagent-Driven (recommended)** — fresh subagent per task. Uses `superpowers:subagent-driven-development`.

**2. Inline Execution** — execute phases in this session with checkpoints. Uses `superpowers:executing-plans`.

**Plan-writing strategy:** Just-in-time per phase (same approach as sub-projects 1–4). Phase 00 fully written now; you approve + execute; Phase 01 written after; repeat.
